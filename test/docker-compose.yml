app:
  build: testapp
  environment:
    - DATABASE_URL=mysql2://test:test@box_testing
    - REDIS_URL=redis://redis
    - SECRET_KEY_BASE=noop
    - TEST_ENV=hey
  ports:
    - '8080:8080'
  links:
    - redis
    - mysql
redis:
  image: redis:3
mysql:
  image: mysql:5.6
